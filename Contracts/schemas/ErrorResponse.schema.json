{
  "$defs": {
    "ApiError": {
      "properties": {
        "code": {
          "$ref": "#/$defs/ErrorCode"
        },
        "message": {
          "title": "Message",
          "type": "string"
        },
        "details": {
          "anyOf": [
            {
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Details"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "title": "ApiError",
      "type": "object"
    },
    "ErrorCode": {
      "enum": [
        "UNKNOWN",
        "INVALID_REQUEST",
        "VALIDATION_FAILED",
        "UNSUPPORTED_MEDIA_TYPE",
        "PAYLOAD_TOO_LARGE",
        "IMAGE_DECODE_FAILED",
        "IMAGE_TOO_SMALL",
        "IMAGE_TOO_LARGE",
        "IMAGE_RESIZE_FAILED",
        "ENGINE_UNAVAILABLE",
        "MODEL_NOT_LOADED",
        "INFERENCE_FAILED",
        "TIMEOUT",
        "INTERNAL_ERROR"
      ],
      "title": "ErrorCode",
      "type": "string"
    }
  },
  "properties": {
    "request_id": {
      "title": "Request Id",
      "type": "string"
    },
    "error": {
      "$ref": "#/$defs/ApiError"
    }
  },
  "required": [
    "request_id",
    "error"
  ],
  "title": "ErrorResponse",
  "type": "object"
}